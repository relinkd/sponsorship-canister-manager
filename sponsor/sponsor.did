type Param = record {
    last_use: nat64;
    count: nat32;
};

service : {
    "get": (text) -> (opt Param) query;
    "insert": (text, Param) -> (opt Param);
    "getSelf": () -> (principal) query;
}